# URL base del backend FastAPI. In locale punta a uvicorn, in produzione
# va impostata su Netlify (Site settings > Environment variables) con
# l'URL pubblico del servizio Railway (es. https://wikiscout-api.up.railway.app)
VITE_API_BASE_URL=http://localhost:8000
